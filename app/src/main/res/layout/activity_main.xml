<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"

    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/minesbackg"
    android:paddingBottom="16dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp"
    tools:context="com.example.mkdan.minessweeper.MainActivity">


    <Button
        android:id="@+id/staertBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="90dp"
        android:radius="100dp"
        android:text="play"
        android:textColor="@android:color/white"
        android:textSize="15sp"
        android:textStyle="bold"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/textView"
        android:layout_marginStart="69dp" />

    <RadioGroup
        android:id="@+id/rg_level"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/staertBtn"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="42dp"
        android:layout_marginStart="35dp"
        android:orientation="vertical">

        <RadioButton
            android:id="@+id/easyBtn"
            android:layout_width="wrap_content"
            android:layout_height="47dp"
            android:layout_above="@+id/medBtn"
            android:layout_alignEnd="@+id/hardBtn"
            android:layout_marginBottom="15dp"
            android:layout_weight="1"
            android:buttonTint="@android:color/holo_green_light"
            android:text="beginner"
            android:textColor="@android:color/white"
            android:textStyle="bold" />


        <RadioButton
            android:text="intermediate"
            android:id="@+id/medBtn"
            android:layout_width="wrap_content"
            android:layout_height="47dp"
            android:layout_above="@+id/hardBtn"
            android:layout_alignStart="@+id/rg_level"
            android:layout_marginBottom="15dp"
            android:buttonTint="@android:color/holo_orange_dark"
            android:textColor="@android:color/white"
            android:textStyle="bold" />

        <RadioButton
            android:text="expert"
            android:id="@+id/hardBtn"
            android:layout_width="wrap_content"
            android:layout_height="47dp"
            android:layout_above="@+id/staertBtn"
            android:layout_alignStart="@+id/medBtn"
            android:layout_marginBottom="15dp"
            android:buttonTint="@android:color/holo_red_dark"
            android:textColor="@android:color/white"
            android:textStyle="bold" />


    </RadioGroup>

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp"
        android:fontFamily="sans-serif-smallcaps"
        android:text="MinesSweeper"
        android:textAppearance="@style/TextAppearance.AppCompat.Light.Widget.PopupMenu.Small"
        android:textColor="@android:color/white"
        android:textSize="36dp"
        android:textStyle="bold"
        tools:layout_width="wrap_content" />

</RelativeLayout>
